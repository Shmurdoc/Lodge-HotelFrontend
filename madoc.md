# Madoc Progress Summary (Frontend-focused)

- Frontend summary: Lodge-HotelFrontend is wired to backend endpoints; JWTs from Supabase are used for Authorization headers; VITE_API_URL controls the backend base URL.
- Local testing: added .env.local to point to a local/mocked backend and a Dockerfile for frontend to run in Docker.
- Next steps: collaborate on production-ready backend dockerization and CI integration; ensure Vercel uses the proper backend URL in production.
